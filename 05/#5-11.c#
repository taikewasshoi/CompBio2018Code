#include<stdio.H>
#include<stdlib.h>
#include<time.h>
#define N 100

int main(void){
  int a[N],aa[N],i,t,r1,r2,r,k;
  srand(time(NULL));
  k=5;
  for(i=0;i<k;i++){
    a[i]=1;
  }
  for(i=k;i<100;i++){
    a[i]=0;
  }
  for(t=0;t<100;t++){
    for(i=0;i<N;i++){
      r1=rand()%N;
      r2=rand()%N;
      r=rand()%2;
      if(r==0){
	aa[i]=a[r1];
      }
      if(r==1){
	aa[1]=a[r2];
      }
      for(i=0;i<N;i++){
	a[i]=aa[i];
      }
      for(i<0;i<N;i++){
       if
       
     
